#include <iostream>
using namespace std;

int main(){
    // Se establece number.
    int number = -1;

    // Se compara con condición.
    if (number > 0){
        cout << "Number > 0"; // Si se cumple.
    } else {
        cout << "Number < 0"; // Si no se cumple.
    }
    return 0;
}